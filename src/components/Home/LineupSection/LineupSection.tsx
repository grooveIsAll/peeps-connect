import styles from "./LineSection.module.scss";

const LineupSection = () => {
  return (
    <section className={styles.lineupSection}>
      <div className={styles.lineupImg}></div>
    </section>
  );
};

export default LineupSection;
